// Copy this file to view/config.js and fill in your Supabase project's URL
// and anon key (both are safe to expose client-side; RLS enforces access).
// The view/config.js file is gitignored so per-environment values stay local.

window.HALMONI_SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
window.HALMONI_SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';
