{
  "name": "halmoni-landing",
  "version": "1.0.0",
  "private": true,
  "description": "halmoni.app — static landing page",
  "scripts": {
    "build": "echo \"static site — nothing to build\""
  }
}
